The following document types were successfully generated using all three approaches:

1. Invoice
2. Badge / Payment Card
3. Agreement (multi-page document)

Status Summary:

- chromedp:
  Successfully generated all document types using HTML templates rendered via headless Chrome.

- maroto:
  Successfully generated all document types using Go-based layout definitions. Fonts were embedded for portability.

- pdfcpu:
  Successfully generated all document types using manual PDF drawing commands and structure handling.

All implementations compiled successfully and produced valid PDF outputs.
